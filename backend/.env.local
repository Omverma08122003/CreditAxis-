# Local development DB (Postgres). Create a local DB 'creditaxis' or change values below.
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/creditaxis
SPRING_DATASOURCE_USERNAME=creditaxis
SPRING_DATASOURCE_PASSWORD=creditaxis_password
SPRING_FLYWAY_ENABLED=false
PORT=10000
